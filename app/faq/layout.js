export const metadata = {
  title: 'FAQ',
  description: 'Frequently asked questions about applying, hosting, and listing on PopUpCo.',
};

export default function FaqLayout({ children }) {
  return children;
}
