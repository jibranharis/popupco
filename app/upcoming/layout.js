export const metadata = {
  title: 'Upcoming events',
  description: 'Browse upcoming pop-up markets and events across the Bay Area.',
};

export default function UpcomingLayout({ children }) {
  return children;
}
